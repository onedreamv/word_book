# word-book

A Typer-based CLI for maintaining a personal word book.

## Quick start

- Install dependencies: `uv sync`
- Run CLI: `uv run wordbook --help`
- Compatibility run mode: `uv run python cli.py --help`
